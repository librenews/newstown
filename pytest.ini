[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: integration tests (slower, require full database)
    requires_api: tests that require external API keys
    slow: slow tests

# Don't run integration tests by default
addopts = -v --strict-markers -m 'not integration'
